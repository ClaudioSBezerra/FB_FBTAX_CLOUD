---
phase: "02-cadastros-base"
plan: "03"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - frontend/src/pages/ContratosPage.tsx
  - frontend/src/App.tsx
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04
  - CONT-05

must_haves:
  truths:
    - "Admin acessa /admin/financeiro/contratos e vê lista de todos os contratos com status"
    - "Admin filtra contratos por cliente via ?cliente_id= na URL ou select na página"
    - "Admin acessa /admin/financeiro/contratos/novo e preenche formulário em 3 etapas"
    - "Etapa 1: selecionar cliente via select ou busca; Etapa 2: marcar CNPJs do grupo cobertos; Etapa 3: selecionar produto+plano e preencher valor_item + data_inicio + periodicidade + valor_total"
    - "Admin clica Salvar e contrato é criado via POST /api/financeiro/contratos com cnpj_ids e itens"
    - "Admin em página de histórico vê contratos de um cliente com status e data de início"
    - "Admin edita observações e periodicidade de contrato ativo via PUT /api/financeiro/contratos"
  artifacts:
    - path: "frontend/src/pages/ContratosPage.tsx"
      provides: "Lista de contratos com filtro por cliente + formulário de novo contrato multi-etapa"
      min_lines: 250
    - path: "frontend/src/App.tsx"
      provides: "Rotas /admin/financeiro/contratos e /admin/financeiro/contratos/novo registradas"
      contains: "ContratosPage"
  key_links:
    - from: "frontend/src/pages/ContratosPage.tsx"
      to: "/api/financeiro/contratos"
      via: "fetch POST com {cliente_id, cnpj_ids, itens, data_inicio, periodicidade, valor_total}"
      pattern: "fetch.*financeiro/contratos"
    - from: "frontend/src/pages/ContratosPage.tsx"
      to: "/api/financeiro/clientes"
      via: "fetch GET para popular select de cliente"
      pattern: "fetch.*financeiro/clientes"
    - from: "frontend/src/pages/ContratosPage.tsx"
      to: "/api/financeiro/produtos"
      via: "fetch GET para popular seleção de produto/plano"
      pattern: "fetch.*financeiro/produtos"
---

## Phase Goal

**As an** admin Fortes Bezerra, **I want to** create contracts linking customers to product plans across multiple CNPJs, **so that** the token engine in Phase 3 has complete contract data to generate and manage access tokens.

<objective>
Criar ContratosPage com duas visões: (1) lista de contratos com filtro por cliente e (2) formulário de novo contrato em 3 etapas (selecionar cliente → marcar CNPJs cobertos → escolher produtos/planos). Registrar rotas em App.tsx. Ao final, admin pode criar contratos completos end-to-end.

Purpose: Completar o último domínio de cadastro (contratos) antes do checkpoint humano em 02-04. O formulário multi-etapa é o componente mais complexo da fase 2 — justifica um plano separado.
Output: 1 página React (ContratosPage com lista + formulário integrado), 2 rotas em App.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-cadastros-base/02-RESEARCH.md
@.planning/phases/02-cadastros-base/02-PATTERNS.md
@.planning/phases/02-cadastros-base/02-01-SUMMARY.md

<interfaces>
<!-- APIs criadas em 02-01. Executor usa sem explorar o codebase. -->

GET /api/financeiro/clientes — retorna todos os clientes (sem filtros) para popular o select
Response: Cliente[]
```typescript
interface Cliente {
  id: string
  razao_social: string
  cnpj: string       // CNPJ principal
  ativo: boolean
}
```

GET /api/financeiro/clientes?id=UUID — retorna cliente com CNPJs do grupo
Response:
```typescript
interface ClienteComCNPJs {
  id: string
  razao_social: string
  cnpj: string
  email?: string
  ativo: boolean
  cnpjs: Array<{ id: string; cnpj: string; descricao?: string; is_principal: boolean }>
}
```

GET /api/financeiro/produtos — retorna produtos com planos
```typescript
interface Plano {
  id: string
  nome: string       // "Lite" | "Standard" | "Premium" | "Enterprise" | "Sob Demanda"
  preco: number | null
}
interface Produto {
  id: string
  codigo: string
  nome: string
  planos: Plano[]
}
```

GET /api/financeiro/contratos?cliente_id=UUID — lista contratos do cliente
Response:
```typescript
interface Contrato {
  id: string
  cliente_id: string
  data_inicio: string    // "2026-01-15"
  periodicidade: string  // "mensal" | "trimestral" | "anual"
  valor_total: number
  status: string         // "ativo" | "encerrado" | "suspenso"
  observacoes?: string
  created_at: string
}
```

POST /api/financeiro/contratos
Body:
```typescript
{
  cliente_id: string
  data_inicio: string       // formato "YYYY-MM-DD"
  periodicidade: string     // "mensal" | "trimestral" | "anual"
  valor_total: number
  observacoes?: string
  cnpj_ids: string[]        // IDs de financeiro.cliente_cnpjs a cobrir
  itens: Array<{
    plano_id: string
    valor_item: number | null
  }>
}
```
Response 201: { id: string }

PUT /api/financeiro/contratos
Body: { id: string, periodicidade: string, valor_total: number, status: string, observacoes?: string }
Response 200: { status: "updated" }

Padrão de useSearchParams do React Router (disponível — importar de react-router-dom):
```typescript
import { useSearchParams, useNavigate } from 'react-router-dom'
const [searchParams] = useSearchParams()
const clienteId = searchParams.get('cliente_id') ?? ''
const navigate = useNavigate()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Tarefa 1: ContratosPage — lista e formulário multi-etapa de novo contrato</name>
  <files>
    frontend/src/pages/ContratosPage.tsx
  </files>
  <action>
Criar frontend/src/pages/ContratosPage.tsx. Esta página tem dois modos controlados por estado: modo lista e modo formulário (ativado quando ?view=novo está na URL ou botão "Novo Contrato" é clicado). Usar `useNavigate` e `useSearchParams` do react-router-dom — já instalado, sem novo pacote.

**Interfaces TypeScript** (definir no topo do arquivo):
```typescript
interface ClienteSimples { id: string; razao_social: string; cnpj: string; ativo: boolean }
interface CNPJ { id: string; cnpj: string; descricao?: string; is_principal: boolean }
interface Plano { id: string; nome: string; preco: number | null }
interface Produto { id: string; codigo: string; nome: string; planos: Plano[] }
interface Contrato {
  id: string; cliente_id: string; data_inicio: string; periodicidade: string
  valor_total: number; status: string; observacoes?: string; created_at: string
}
interface ItemSelecionado { plano_id: string; valor_item: string }
```

**Estado da página:**
- `view: 'lista' | 'novo'` — modo atual
- `clientes: ClienteSimples[]` — carregados no mount para o select
- `contratos: Contrato[]` — lista de contratos (filtrada por cliente)
- `clienteIdFiltro: string` — ID do cliente selecionado no filtro de lista
- Formulário etapa 1: `etapa: 1 | 2 | 3`, `clienteSelecionadoId: string`, `cnpjsDoGrupo: CNPJ[]` (carregados ao selecionar cliente), `cnpjsSelecionados: string[]` (IDs marcados), `produtos: Produto[]` (carregados na etapa 3), `itensSelecionados: ItemSelecionado[]`, `dataInicio: string`, `periodicidade: 'mensal' | 'trimestral' | 'anual'`, `valorTotal: string`, `observacoes: string`
- `loading: boolean`, `submitting: boolean`, `error: string | null`

**Mount (useEffect vazio):** Carregar `clientes` via GET /api/financeiro/clientes. Carregar `produtos` via GET /api/financeiro/produtos. Checar `searchParams.get('view') === 'novo'` → se sim, setView('novo').

**Modo LISTA:**
- Select "Filtrar por cliente" com options de `clientes`. Ao mudar, carregar contratos via GET /api/financeiro/contratos?cliente_id=ID.
- Tabela com colunas: Data Início, Periodicidade, Valor Total (formatar como "R$ X.XXX,XX"), Status (Badge), Ações (botão "Editar").
- Botão "Novo Contrato" no header que chama `setView('novo'); setEtapa(1)`.
- Edição: ao clicar "Editar", abrir Dialog com campos periodicidade, valor_total, status e observacoes. PUT e atualizar lista.

**Modo NOVO CONTRATO — Formulário em 3 etapas:**

Exibir stepper simples (texto "Etapa 1 de 3 — Selecionar Cliente") acima do formulário.

Etapa 1 — Selecionar Cliente:
- Select com todos os clientes ativos. Ao selecionar, chamar GET /api/financeiro/clientes?id=ID para obter CNPJs do grupo e setar `cnpjsDoGrupo`.
- Botão "Próximo" desabilitado até clienteSelecionadoId preenchido.

Etapa 2 — Selecionar CNPJs cobertos:
- Listar `cnpjsDoGrupo` como checkboxes. Label: `cnpj.cnpj + (cnpj.is_principal ? ' (Principal)' : cnpj.descricao ? ' — ' + cnpj.descricao : '')`.
- `cnpjsSelecionados: string[]` armazena os IDs marcados.
- Botão "Próximo" desabilitado se nenhum CNPJ marcado. Botão "Voltar" retorna à etapa 1.

Etapa 3 — Produtos, datas e valores:
- Para cada produto em `produtos`, renderizar seção colapsável (simples: `<details><summary>`) com os planos como checkboxes. Ao marcar um plano, adicionar `{plano_id, valor_item: ''}` em `itensSelecionados`. Ao desmarcar, remover. Input de `valor_item` exibido ao lado de cada plano marcado (string para aceitar vazio = null).
- Campos adicionais: data_inicio (input type="date"), periodicidade (select: mensal/trimestral/anual), valor_total (input numérico), observacoes (textarea opcional).
- Botão "Criar Contrato" desabilitado se: nenhum item selecionado, ou data_inicio vazia, ou valor_total vazio.

**handleSubmitContrato:** Montar body com `cliente_id, data_inicio, periodicidade, valor_total: parseFloat(valorTotal), observacoes, cnpj_ids: cnpjsSelecionados, itens: itensSelecionados.map(i => ({plano_id: i.plano_id, valor_item: i.valor_item ? parseFloat(i.valor_item) : null}))`. Chamar POST /api/financeiro/contratos. Após 201 → toast.success("Contrato criado com sucesso") → setView('lista') → recarregar contratos do cliente criado.

**Layout:** `div className="min-h-screen bg-gray-50 p-8"` → `div className="max-w-4xl mx-auto space-y-6"` → heading "Contratos" com botão condicional. Card com conteúdo do modo atual.
  </action>
  <verify>
    <automated>cd /home/claudio/projetos/FB_FBTAX_CLOUD && npm run build --prefix frontend 2>&1 | tail -20</automated>
  </verify>
  <done>npm run build exit 0 sem erros TypeScript. ContratosPage.tsx existe e compila. Arquivo tem mais de 250 linhas.</done>
</task>

<task type="auto" tdd="false">
  <name>Tarefa 2: Registrar rotas de contratos em App.tsx</name>
  <files>
    frontend/src/App.tsx
  </files>
  <action>
Editar frontend/src/App.tsx para adicionar o import e a rota de ContratosPage.

**Import a adicionar** (após os imports de ClientesPage e ProdutosPage adicionados em 02-02):
```typescript
import ContratosPage from './pages/ContratosPage'
```

**Rota a adicionar** (no bloco {/* Financeiro */}, após as rotas de clientes e produtos):
```typescript
<Route path="/admin/financeiro/contratos" element={
  <ProtectedRoute>
    <ContratosPage />
  </ProtectedRoute>
} />
```

Uma única rota `/admin/financeiro/contratos` cobre tanto a lista quanto o formulário de novo contrato — o modo é controlado por estado interno da página (via `?view=novo` em query param ou toggle de estado). Não criar rota separada `/admin/financeiro/contratos/novo`.

Manter todas as demais rotas exatamente como estão.
  </action>
  <verify>
    <automated>cd /home/claudio/projetos/FB_FBTAX_CLOUD && grep -c "ContratosPage" frontend/src/App.tsx && npm run build --prefix frontend 2>&1 | tail -10</automated>
  </verify>
  <done>App.tsx tem 2 ocorrências de ContratosPage (1 import + 1 no elemento Route). npm run build exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Usuário → Formulário de contrato | Input de valor_total, valor_item, data_inicio são strings não validadas no frontend |
| Frontend → Backend | POST de contrato envia arrays de IDs; backend valida existência via FK |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Tampering | handleSubmitContrato | mitigate | parseFloat + NaN check antes de enviar; backend valida FKs em transação — IDs inválidos causam rollback |
| T-02-11 | Information Disclosure | Select de clientes carrega todos | accept | Lista de clientes só acessível a admins autenticados (ProtectedRoute + withAuth no backend) |
| T-02-12 | Denial of Service | Múltiplos fetches simultâneos no mount | accept | Dois fetches paralelos (clientes + produtos) no mount — volume pequeno no v1, sem throttle necessário |
| T-02-SC | Tampering | nenhum novo pacote npm neste plano | accept | Sem novas dependências — react-router-dom já instalado |
</threat_model>

<verification>
Verificação final do plano:

```bash
# 1. Build TypeScript limpo
cd /home/claudio/projetos/FB_FBTAX_CLOUD && npm run build --prefix frontend

# 2. Página criada
ls frontend/src/pages/ContratosPage.tsx

# 3. Rota registrada
grep "financeiro/contratos" frontend/src/App.tsx

# 4. Import registrado
grep "ContratosPage" frontend/src/App.tsx

# 5. POST de contrato presente
grep "financeiro/contratos" frontend/src/pages/ContratosPage.tsx | head -5

# 6. Formulário multi-etapa — etapas presentes
grep "etapa\|Etapa" frontend/src/pages/ContratosPage.tsx | head -10

# 7. cnpj_ids e itens no body do POST
grep "cnpj_ids\|itens" frontend/src/pages/ContratosPage.tsx
```
</verification>

<success_criteria>
- npm run build exit 0 sem erros TypeScript
- ContratosPage.tsx renderiza lista de contratos com filtro por cliente
- Formulário de novo contrato tem 3 etapas claras: selecionar cliente → marcar CNPJs → escolher produtos/planos + dados financeiros
- POST enviado para /api/financeiro/contratos contém cliente_id, cnpj_ids (array), itens (array de {plano_id, valor_item}), data_inicio, periodicidade, valor_total
- Edição de contrato existente via Dialog com PUT para periodicidade, valor_total, status, observacoes
- Rota /admin/financeiro/contratos registrada em App.tsx dentro de ProtectedRoute
- Nenhum uso de react-hook-form ou TanStack Query
</success_criteria>

<output>
Criar `/home/claudio/projetos/FB_FBTAX_CLOUD/.planning/phases/02-cadastros-base/02-03-SUMMARY.md` ao concluir.
</output>
